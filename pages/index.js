import Head from "next/head";
import Image from "next/image";
import Navbar from "../component/Navbar";
import styles from "../styles/Home.module.css";

export default function Home() {
  return (
    <div className={styles.container}>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <Navbar isTransparent={true} />

      <main className={styles.main}>
        <section id={styles.heroSection} className="bg-gradient">
          <div className="wrapper two-section-wrapper ">
            <div className="left">
              <h2 className={styles.heroHeading}>Wide Range of Classic BANDHAJ Apparels</h2>
              <button className={"btn " + styles.exploreBtn}>Explore Now</button>
            </div>

            <div className="right">
              <div className={styles.imageWrapper}>
                <Image
                  className={styles.heroImage}
                  src="/assets/images/dupata_1.jpeg"
                  width={150}
                  height={100}
                  layout="responsive"
                />
              </div>
            </div>
          </div>
        </section>
      </main>
    </div>
  );
}
